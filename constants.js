export const DB_NAME = 'Translate_Quran_App_DB';

export const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const passwordPattern = /^.{8,}$/; // atleast 8 characters long

export const cookieOptions = {
    httpOnly: true,
    secure: true
};


